- js_form

= simple_form_for caja do |form|

  = render 'shared/errors', record: caja

  = form.input :titulo
  = form.input :cuerpo
  = form.association :paginas, as: :check_boxes

  .actions
    = form.button :submit
