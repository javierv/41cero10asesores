- js_form
- bsmselect_con_cajas

= simple_form_for @pagina do |form|

  = render 'shared/errors', :record => @pagina

  = form.input :titulo, :label => 'Titulo'
  = form.input :cuerpo, :label => 'Cuerpo'

  = hidden_field_tag "pagina[caja_ids][]"
  = form.association :cajas, :input_html => {:class => 'cajas'}

  .actions
    = form.button :submit